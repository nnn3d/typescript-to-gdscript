// @gd.eval: @tool
export class __CLASS__ extends Node {
  // @gd.eval: @icon("res://icon.svg")
  speed: float = 10.0;

  // @gd.eval: signal custom_signal(value: int)
  health: int = 100;

  _ready() {
    // @gd.eval: var special := preload("res://special.tscn")
    let x: int = 1;
    // @gd.eval:	indented_with_tab()
  }
}
